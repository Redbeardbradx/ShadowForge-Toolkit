# Open notepad: notepad pyproject.toml
# Paste this full verse (select all, copy from here to end):
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "shadowforge"
version = "0.1.0"
description = "Utah Viking's ethical pentest toolkit—scan, payload, shield."
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "rich>=13.0",
    "requests>=2.28",
    "scapy>=2.4"
]
authors = [
    {name = "Redbeard Brad", email = "your@email.com"}
]
license = {text = "MIT"}
keywords = ["pentest", "ethical-hacking", "network-scan"]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "stem>=1.8"
]

[project.scripts]
shadowforge = "shadowforge.main:cli_entry"
# Full verse check (paste if needed, but just fix scripts block):